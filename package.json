{
  "name": "my-portfolio",
  "private": true,
  "version": "0.0.0",
  "homepage": "https://vadaszgergo.github.io/vadaszgergo.github.io/",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  // ... rest of the file stays the same
} 